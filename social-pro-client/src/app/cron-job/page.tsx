import { FC, ReactNode } from 'react';
import { CronJobAddForm } from '@/components/features/cron-job/add-form';

const CronJobPage: FC = (): ReactNode => {
  return <CronJobAddForm />;
};

export default CronJobPage;