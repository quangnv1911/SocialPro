/* eslint-disable no-unused-vars */
export enum DetectorType {
  RegToString = 0, // Check according to regular
  Performance = 1, // Performance detection based on log big data
  DebugLib = 2, // Detect third-party debugging tools eruda and vconsole
}
